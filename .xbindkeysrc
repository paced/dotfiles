# Control playback and notify.
# Coming soon.

"mpc toggle && mpdnotify"
  Control + Mod2 + KP_Begin

# Keyboard backlight settings
"asus-kbd-backlight up"
  XF86KbdBrightnessUp

"asus-kbd-backlight down"
  XF86KbdBrightnessDown

# Laptop brightness settings.
"xbacklight -inc 10"
  XF86MonBrightnessUp

"xbacklight -dec 10"
  XF86MonBrightnessDown

# Laptop sound settings.
"amixer -q sset Master toggle"
  XF86AudioMute

"amixer -q sset Master 3%-"
  XF86AudioLowerVolume

"amixer -q sset Master 3%+"
  XF86AudioRaiseVolume

# Printscreen.
"drawmaim -f"
  Print

# Printscreen all workspaces.
"drawmaim -d"
  Control + Print

# Printscreen region.
"drawmaim -s"
  Control + Shift + 2

# Launch hotkeys. Edit coming soon.
"rofi -show run -font "gohufont 9" -width 320"
  F2
